<% if flash.any? %>
  <div class="flash-messages">
    <% flash.each do |type, message| %>
      <div class="flash-<%= type %> data-turbo-temporary">
        <%= message %>
      </div>
    <% end %>
  </div>
<% end %>
